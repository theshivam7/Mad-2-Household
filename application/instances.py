# from flask_caching import Cache

# cache = Cache()
from flask_caching import Cache

# Initialize a Cache instance that will be configured later with the Flask app
# This follows a factory pattern where initialization and configuration are separate
cache = Cache()